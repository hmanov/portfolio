import Link from 'next/link';
import styles from '../styles/Footer.module.css';
const { footer, container } = styles;
const Footer = () => {
    return (
        <footer className={footer}>
            <div className={container}>fooaater</div>
        </footer>
    );
};

export default Footer;
